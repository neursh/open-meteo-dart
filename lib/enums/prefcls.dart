/// Settings provided by Open-Meteo.
enum TemperatureUnit { celsius, fahrenheit }

/// Settings provided by Open-Meteo.
enum WindspeedUnit { kmh, ms, mph, kn }

/// Settings provided by Open-Meteo.
enum PrecipitationUnit { mm, inch }

/// Settings provided by Open-Meteo.
enum CellSelection { land, sea, nearest }

/// Settings provided by Open-Meteo.
enum LengthUnit { metric, imperial }
